[package]
name = "breviarium"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dioxus = { version = "0.7.0", features = ["fullstack"] }

[features]
default = []
server = ["dioxus/server"]
web = ["dioxus/web"]
